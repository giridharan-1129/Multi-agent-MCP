# ===================================================================
# FastAPI Multi-Agent Repository Chat System - Environment Variables
# ===================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env in the root directory
# 2. Replace placeholder values with your actual credentials
# 3. Never commit .env to version control
# 
# ===================================================================

# Application Environment
# Options: development, testing, production
ENVIRONMENT=development

# ===================================================================
# Neo4j Database Configuration
# ===================================================================
# 
# Neo4j is used to store the knowledge graph of the codebase.
# It represents classes, functions, and their relationships.
#
# Default connection (local Docker container):
#   URI: bolt://localhost:7687
#   Username: neo4j
#   Password: password (change this!)
#
# For remote Neo4j:
#   URI: bolt://your-neo4j-host:7687 or neo4j://your-host:7687
#

# Neo4j server URI
# Format: bolt://host:port or neo4j://host:port
NEO4J_URI=bolt://localhost:7687

# Neo4j authentication username
NEO4J_USERNAME=neo4j

# Neo4j authentication password
# IMPORTANT: Change this in production!
NEO4J_PASSWORD=password

# Neo4j database name (default: neo4j)
NEO4J_DATABASE=neo4j

# ===================================================================
# OpenAI Configuration
# ===================================================================
#
# Used for LLM-powered code analysis and response generation.
#
# To get your API key:
# 1. Go to https://platform.openai.com/api-keys
# 2. Create a new API key
# 3. Copy it here
#
# Available models:
#   - gpt-4 (latest, most capable)
#   - gpt-4-turbo-preview (faster, cheaper)
#   - gpt-3.5-turbo (fastest, cheapest)
#

# Your OpenAI API key
# Get from: https://platform.openai.com/api-keys
# IMPORTANT: Keep this secret! Never commit to version control
OPENAI_API_KEY=your-openai-api-key-here

# Which OpenAI model to use
# Options: gpt-4, gpt-4-turbo-preview, gpt-3.5-turbo
OPENAI_MODEL=gpt-4

# Temperature controls randomness in responses
# Range: 0.0 (deterministic) to 1.0 (creative)
# Recommended for code analysis: 0.3-0.7
OPENAI_TEMPERATURE=0.7

# Maximum tokens in response
# Each token ≈ 4 characters
# gpt-4 max: 8000 tokens for input+output
# gpt-3.5-turbo max: 4096 tokens
OPENAI_MAX_TOKENS=2000

# ===================================================================
# MCP Server Configuration
# ===================================================================
#
# MCP = Model Context Protocol
# These settings apply to all MCP agents.
#

# Host that MCP servers bind to
# 0.0.0.0 = accessible from any machine
# localhost = only local access
MCP_HOST=localhost

# Base port for MCP servers
# Individual agents increment from this:
# - Orchestrator: MCP_PORT
# - Indexer: MCP_PORT + 1
# - Graph Query: MCP_PORT + 2
# - Code Analyst: MCP_PORT + 3
MCP_PORT=8000

# Request timeout in seconds
# How long to wait for agent response before giving up
MCP_TIMEOUT=30

# Number of retry attempts for failed requests
MCP_RETRY_ATTEMPTS=3

# ===================================================================
# Repository Configuration
# ===================================================================
#
# Settings for repository indexing and parsing.
#

# Default GitHub repository to index
# Change this to analyze a different repository
REPO_DEFAULT_REPO_URL=https://github.com/tiangolo/fastapi

# Where to clone repositories locally
# Must have write permissions to this directory
REPO_CLONE_PATH=/tmp/repositories

# Maximum file size to process in MB
# Large files are skipped to prevent memory issues
# Recommended: 5-20 MB
REPO_MAX_FILE_SIZE_MB=10

# ===================================================================
# Logging Configuration
# ===================================================================
#
# Control how the system logs information.
#

# Logging level
# DEBUG = very detailed, shows everything
# INFO = normal operation, shows important events
# WARNING = only warnings and errors
# ERROR = only errors
# CRITICAL = only critical errors
LOG_LEVEL=INFO

# Log output format
# json = machine-readable JSON format (better for production)
# text = human-readable text format (better for development)
LOG_FORMAT=json

# ===================================================================
# FastAPI Gateway Configuration
# ===================================================================
#
# The gateway is the main API server that users interact with.
#

# Host the gateway binds to
# 0.0.0.0 = accessible from any machine
# localhost = only local access
GATEWAY_HOST=0.0.0.0

# Port the gateway listens on
# Access API at: http://localhost:8000
GATEWAY_PORT=8000

# Enable auto-reload when code changes (development only!)
# Set to false in production for performance
GATEWAY_RELOAD=true

# ===================================================================
# Conversation Management
# ===================================================================
#

# Number of conversation messages to keep in memory
# Older messages are kept in database but not in fast memory
# Higher = more context but more memory usage
CONVERSATION_MEMORY_SIZE=50

# Session timeout in minutes
# Users session will expire after this duration of inactivity
SESSION_TIMEOUT_MINUTES=30

# ===================================================================
# Optional: Individual Agent Ports
# ===================================================================
#
# If you want agents on specific ports instead of incrementing,
# uncomment and set these:
#

# ORCHESTRATOR_PORT=8001
# INDEXER_PORT=8002
# GRAPH_QUERY_PORT=8003
# CODE_ANALYST_PORT=8004

# ===================================================================
# Docker Configuration (Optional)
# ===================================================================
#
# These are used when running with Docker Compose.
#

# Docker network name
# DOCKER_NETWORK=multiagent-network

# Docker Neo4j image
# DOCKER_NEO4J_IMAGE=neo4j:5.14

# ===================================================================
# Advanced Settings (Optional)
# ===================================================================
#

# Request ID header name (for tracing)
# REQUEST_ID_HEADER=X-Request-ID

# Enable CORS (Cross-Origin Resource Sharing)
# CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080"]

# Enable detailed error messages (development only!)
# DEBUG_MODE=false

# Maximum concurrent agent tasks
# MAX_CONCURRENT_AGENTS=10

# ===================================================================
# Notes
# ===================================================================
#
# - All boolean values should be: true or false
# - All numbers without quotes
# - All strings with or without quotes (works either way)
# - Comments starting with # are ignored
# - Blank lines are ignored
#
# Example valid formats:
#   STRING_VALUE=hello
#   STRING_VALUE="hello"
#   NUMBER_VALUE=42
#   BOOLEAN_VALUE=true
#   BOOLEAN_VALUE=false
#
